# 媒资资料库 API · 宝塔部署指南

影视 / 游戏 / 动漫 / 短剧 / 综艺 统一资料库，对外提供 **TMDB 兼容代理**（ZBLOK 等主题零改动对接）+ **统一 API** + **可视化管理后台**，按 **短剧 / 电影 / 电视剧 / 动漫 / 综艺 / 游戏** 6 大分类组织，每条可后台编辑并生成静态内页。下载仅提供网盘链接（多条，可自定义网盘类型）。

---


## PHP 8.2 与发布保护

本版本运行基线为 PHP 8.2+，不再兼容 PHP 5.6、7.x 或 8.0/8.1。发布目录中的 PHP 文件经过 AST 混淆和压缩，用于降低直接阅读成本，但这不等同于编码加密。

Nuitka 是 Python 编译器，不能编译或加密本项目的 PHP 文件。若需要真正的 PHP 编码保护，应使用支持 PHP 8.2 的 ionCube Encoder + ionCube Loader，或 SourceGuardian + 对应 Loader。编码器必须在构建机执行，宝塔服务器只安装与 PHP 8.2、FPM/CLI SAPI 和 CPU 架构匹配的 Loader。

当前工作区未包含商业编码器及授权文件，因此本版不会伪称为“加密包”。部署前请在宝塔 PHP 8.2 的 FPM 和 CLI 分别确认 Loader 已加载；计划任务执行的 `cron/*.php` 使用 CLI PHP，也必须能加载 Loader。

## 一、前置条件

| 项 | 要求 |
|---|---|
| 面板 | 宝塔等任意 LNMP 面板 |
| **PHP** | **8.2+**（推荐 8.3 或 8.4）|
| MySQL | 5.7+（推荐 8.0+）|
| 扩展 | `pdo_mysql`（必需）、`curl`（必需）、`json`（必需）、`mbstring`、`openssl` |
| 目录 | `config/` 与站点根需可写（安装器要写配置、生成器要写 `uisc/`） |
| Key | TMDB：https://www.themoviedb.org/settings/api ；RAWG（游戏，可选）：https://rawg.io/apidocs ；红果短剧需配置 |

---

## 二、全新安装（网页 / 命令行，二选一）

> 🔒 **关于网页安装器**：`install.php` 是**带自锁**的 —— 只要站点已安装（存在
> `config/install.lock`，或 `config/config.php` 的 `db` 段已填真值），它对所有人返回 **404**，
> 与文件不存在完全一样，没人能借此重装站点或读到服务器信息。只有在"尚未安装"时它才显示安装向导。
> 不想留它？装完直接删掉站点根的 `install.php` 即可，功能不受任何影响。
> 其余安装 / 初始化类路径（`/setup`、`/upgrade`、`/update`、`install.php.bak`…）仍然一律 404。

### 方式一：网页安装（推荐，不用终端）

1. **上传代码**：把整个目录传到站点根目录（如 `/www/wwwroot/cs.zyfx.wang/`）。
2. **建库**：宝塔 → 数据库 → 添加数据库（记下库名 / 用户名 / 密码）。
3. **打开安装向导**：浏览器访问

   ```
   https://你的域名/install.php
   ```

   页面会先做**环境自检**（PHP 版本、pdo_mysql / curl / json 扩展、目录可写性），
   不通过的项目标红并直接给出修法。也可以从首页的「🚀 开始网页安装」按钮点进来。
4. **填表**：数据库地址 / 端口 / 库名 / 用户名 / 密码，站点名称与网址，
   后台管理令牌（留空自动生成 16 位随机串，也可点「生成一个」），TMDB / RAWG Key 可留空，勾选启用的数据源。
5. **点「开始安装」**，自动完成：环境自检 → 试连数据库 → **建表**
   （`media_items` / `sync_log` / `link_checks` / `app_settings` / `app_users` / `app_requests`）
   → 生成 `config/config.php` 与 `config/pan_types.php` → 创建 `uisc/` → 写 `config/install.lock`。
   成功页会把**后台令牌**、**后台地址**、**自检地址**一并给你，并列出接下来该做的 5 件事。
6. 装完再访问 `/install.php` 就变成 **404** 了 —— 这是**正确现象**，说明自锁已生效。

> 安装器里填的**后台令牌 / TMDB Key / RAWG Key / 启用数据源 / 网盘类型**，装好后都能在
> **后台 →「系统设置」** 里随时增删改（保存到数据库，见第十一节），不必再回来改 `config.php`，升级覆盖也不会丢。

### 方式二：命令行安装（适合重装 / 迁移 / 没有 web 环境）

1. **上传代码**：把整个目录传到站点根目录（如 `/www/wwwroot/cs.zyfx.wang/`）。
2. **建库**：宝塔 → 数据库 → 添加数据库（记下库名 / 用户名 / 密码）。
3. **打开终端**（宝塔 → 终端，或 SSH），进入站点目录执行：

   ```bash
   cd /www/wwwroot/cs.zyfx.wang
   php tools/install-cli.php
   ```

   按提示依次输入：数据库地址（默认 `127.0.0.1`）、端口（`3306`）、库名、用户名、密码、后台令牌（留空会自动生成一个随机令牌）、TMDB / RAWG Key（可留空）。

   也可以全参数式一把过（适合脚本化部署 / 迁移）：

   ```bash
   php tools/install-cli.php --db=media_library --user=media_lib --pass=你的密码 --token=我的后台令牌
   ```

4. 安装器会自动完成：环境自检 → 测试数据库连接 → **自动建表**（`media_items` / `sync_log` / `link_checks` / `app_settings` / `app_users` / `app_requests`）→ 生成 `config/config.php` 与 `config/pan_types.php` → 创建 `uisc/` → 写 `config/install.lock` 锁定安装。结束时会把**后台地址**与**自检地址（已带令牌）**打印出来。
5. 访问 `https://你的域名/check.php?token=你的后台令牌` 体检一遍（应全绿），再到 `https://你的域名/admin` 用令牌进后台。

> **两份安装器是同一套内核**（`core/Installer.php`）：建表 SQL、生成的配置内容、安装锁完全一致，
> 用哪个装上来的结果都一样。命令行版额外支持 `--yes` / `--force` 等参数。

> **重复安装 / 换服务器**：命令行 `php tools/install-cli.php --force`（原 `config.php` 会先备份为 `config.php.bak-日期时间`）；
> 网页重装则先删掉 `config/install.lock` 与 `config/config.php`，安装向导会重新可用。
> 安装器自包含建表 SQL，**不依赖 `sql/` 目录**，也不需要先手动建库。
> 发布包**不含 `config/config.php`**（只带示例 `config/config.sample.php`），该文件由安装器生成——这样升级覆盖源码时，就不会把你的数据库口令 / API Key 冲掉。
> 安装器会另外写一份 `config/install-info.txt`（里面有后台令牌）。它放在 `config/` 下，公网访问不到（该目录被 404），**但仍建议记下令牌后删掉它**。

### 升级已有站点（覆盖源码）

1. 把新包解压覆盖到站点根，**保留**这几项不要动：
   - `config/config.php`（你的数据库与密钥）
   - `config/install.lock`（安装标记）
   - `config/pan_types.php`（如果你自己加过网盘类型）
   - `uisc/`（已生成的静态内页）
2. 若本次有 SQL 变更（看 `CHANGELOG.md`），在宝塔 phpMyAdmin 里导入对应的 `sql/migrate_*.sql`。
3. 访问一次 `check.php?token=你的后台令牌` 确认全绿；不需要重新安装。

> 发布包本身已不含 `config/config.php` 与 `install.lock`，直接整包覆盖也不会重置配置；`config/pan_types.php` **是**会随包覆盖的（新增网盘类型就靠它），如你自定义过请先备份。
> **后台「系统设置」改过的东西不会受覆盖影响**：它们存在数据库 `app_settings` 表里，优先级高于配置文件（见第十一节）。

---

## 三、手动安装（不想用安装器时）

1. **上传代码**：整个目录传到站点根。
2. **建库**：宝塔 → 数据库 → 新建（如 `media_library`）→ 导入 `sql/install.sql`。
   - 旧版升级：依次按需导入 `sql/migrate_metrics.sql` → `migrate_meta.sql` → `migrate_downloads.sql` → `migrate_link_checks.sql` → `migrate_settings.sql`（都是 `ALTER/ADD/CREATE`，数据不丢）。
   - `migrate_settings.sql` 可以**不手工导入**：打开后台「系统设置」时会自动建该表。
3. **填配置**：把 `config/config.sample.php` 复制/改名为 `config/config.php`，再编辑
   - `db.*` 填 MySQL 账号
   - `tmdb.api_key` / `rawg.api_key` 填 Key（不用游戏就把 `adapters` 里的 `rawg` 去掉）
   - `admin.token` 改成你自己的随机串（后台登录用）

   > 以上除 `db.*` 外都可以留空/随便填，**装完在后台「系统设置」里改**（会覆盖配置文件，见第十一节）。
4. **建 `uisc/` 目录**（生成静态内页用，可写）。
5. **配置 PHP**：站点 → 设置 → 确认 `curl`、`json`、`pdo_mysql` 已开；关闭「防跨站」（或把 `open_basedir` 放开到站点目录）。
6. **配置伪静态**（见下一节，必做）。
7. **验证**：`https://你的域名/3/configuration` 应返回 TMDB 配置 JSON；`/admin` 进后台。

---

## 四、Nginx 伪静态 + 安全加固（必做）

本项目所有动态请求都由根目录的 `index.php` 转发，**必须加第一条重写规则**，否则 `/admin`、`/api/v1/*`、`/3/*`、`/check`、`/track` 等都会 404。

**宝塔 → 网站 → 设置 → 伪静态**，粘贴：

```nginx
# 1) 动态路由统一交给 index.php（/uisc/xxx.html 等真实静态文件仍直接返回，不进 PHP）
location / {
    try_files $uri $uri/ /index.php?$query_string;
}

# 2) 保护敏感目录（⚠️ 必须用「^~ 前缀匹配」）
#    原因：宝塔默认给站点配的 PHP 处理段是正则 location ~ [^/]\.php(/|$)，
#    而 Nginx 的匹配优先级是「精确 = > ^~ 前缀 > 正则 > 普通前缀」。
#    如果这里写成普通前缀 location /config/，会被上面那条 .php 正则抢先命中，
#    结果 /config/config.php 仍然被 PHP 执行（照旧能被公网打开）——用 ^~ 才拦得住。
location ^~ /config/   { return 404; }
location ^~ /sql/      { return 404; }
location ^~ /core/     { return 404; }
location ^~ /cron/     { return 404; }
location ^~ /adapters/ { return 404; }
location ^~ /tools/    { return 404; }

# 3) 禁止下载源码 / 文档 / 备份 / 锁文件（这些不是 .php，用正则即可）
location ~* \.(sql|md|lock|bak|ini|log|env|sh|yml|yaml|conf|sample)$ {
    return 404;
}

# 4) 安装路径黑洞：setup / upgrade / update 等一律 404
#    ⚠️ 这里**故意没有写 install** —— 网页安装器 install.php 由它自身判定：
#    已安装就返回 404，未安装才给安装向导。若在 Nginx 层把它拦死，
#    未安装的站点就永远装不了了。install.php.bak 之类仍会被下面这条正则拦住。
location ~* ^/(setup|upgrade|update) {
    return 404;
}

# 5) PHP 由面板自带的配置处理，无需在此重复写 fastcgi
```

**要点**

- 第 1 条是核心：`try_files` 先找真实文件/目录，找不到才转给 `index.php`。
  所以 `/uisc/123.html` 这类静态内页会被 Nginx 直接吐出去，**不消耗 PHP**。
- 第 2 条的目录保护**一定要用 `^~`**。实测过：写成普通前缀 `location /config/` 时，`/config/config.php` 依然会被 PHP 执行（返回 200），等于把数据库配置文件暴露在公网入口上；换成 `location ^~ /config/` 后才会真正 404。
- 第 3~4 条属于**安全加固**。即使不写，程序内部也有三层兜底，但写在 Nginx 上最彻底：
  1. `core/Guard.php`（入口守卫）：`/setup`、`/upgrade`、`/config/`、`/sql/` 等路径一律返回 404；`/install.php` 则放行给安装器自身判定（已安装即 404）、
  2. 每个内部 `.php` 文件顶部的 `ml_guard_shield()`：**直接用网址请求该文件也返回 404**；
  3. `config/ sql/ core/ adapters/ cron/ tools/` 里各有一个"拒访 `index.php`"，目录级访问一律 404。
- **不要**套用面板里 ThinkPHP/Laravel 的预设伪静态，本项目不是那些框架。
- 换 PHP 版本后如不生效，重启一次 PHP 或清一下面板的「网站缓存」。

### 安全加固清单（v1.4.0 起，v1.6.3 更新安装器口径）

| 项 | 状态 | 说明 |
|---|---|---|
| 网页安装器 `install.php` | **带自锁**（v1.6.3） | 仅"尚未安装"时可用；一旦写入 `config/install.lock`（或 config.php 的 db 已填真值），对所有人返回 404，与文件不存在无异，无法被用来重装站点。不想留可直接删除 |
| 安装类路径 `/setup` `/upgrade` `/update` | **已黑洞** | 即使这些文件被误传回站点也不会执行，直接 404（`install.php.bak` 同样被拦） |
| 命令行安装器 `tools/install-cli.php` | **仅命令行** | 公网访问一律 404；与网页安装器共用 `core/Installer.php` 内核 |
| 敏感目录访问 | **三层拦截** | Nginx `location ^~ /目录/` + `core/Guard.php` + 目录拒访 `index.php` |
| 定时脚本 `cron/*.php` | **已加守卫** | 被网址请求一律 404，防止任何人远程触发采集 / 重建静态页（v1.4.0 起） |
| 内部 PHP 文件直访 | **已拦截** | `core/` `adapters/` `api/` 下每个文件都带 `ml_guard_shield()`，直访 404 |
| 自检页 `check.php` | **未授权即 404** | 不带正确令牌时不会返回"需要令牌"页，扫描器无法确认该文件存在 |
| `config/` `sql/` 文件下载 | **已拦截** | `.sql / .md / .lock / .bak` 等后缀一律拒；`.htaccess` 同步保护 |
| 静态页埋点 `/track?id=N` | 公开端点 | 只做一次点击数 `UPDATE` 并返回 204，不输出任何站点信息 |

> 部署完成后，打开 **后台 →「安全防护」** 或 `check.php` 的「8 · 安全防护」分组，可一键核对上面每一项是否到位（含对 `/install.php`、`/sql/install.sql` 的公网可达性实测 —— `/install.php` 在"已安装"状态下**必须**是 404）。

---

## 五、Apache 环境（非宝塔 Nginx 时）

站点根放一个 `.htaccess`（发布包已带，可直接用）：

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ index.php [QSA,L]
  # 网页安装器 install.php：放行，由它自己判定（已安装 → 主动 404；未安装 → 安装向导）
  RewriteRule ^install\.(php)?$ - [L]
  # 安装路径黑洞：其余 install / setup / upgrade / update 类路径一律 404
  RewriteRule ^(install|setup|upgrade|update)([._-][a-z0-9]+)?(\.[a-z0-9]+)?(/.*)?$ - [R=404,L]
</IfModule>

RedirectMatch 404 ^/(config|sql|cron|core|adapters|tools|data|logs|backup)/

<FilesMatch "\.(sql|md|lock|bak|ini|log|env|sh|yml|yaml|conf|sample)$">
  Require all denied
</FilesMatch>

<FilesMatch "^\.">
  Require all denied
</FilesMatch>
```

---

## 六、给 ZBLOK / 影视主题对接

把主题里的 TMDB API 地址（base URL）从 `https://api.themoviedb.org/3` 改成你的 `https://你的域名` 即可。
代理会：命中本地缓存直接返回 → 未命中/过期则回源 TMDB 并写入本地 → 原样回吐官方 JSON，**主题无需改任何字段**。
图片仍走 `image.tmdb.org`（与官方一致），无需额外带宽。

---

## 七、统一 API（跨源，自己前端/插件用）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v1/search?q=xxx&type=movie&source=tmdb&page=1` | 本地跨源搜索 |
| GET | `/api/v1/items?type=game&page=1&q=` | 本地资料库列表 |
| GET | `/api/v1/item/{source}/{type}/{id}` | 单条原始数据 |
| GET | `/api/v1/trending` | 各源热门汇总 |
| GET | `/api/v1/genres` | 全部类型标签 |
| GET | `/api/v1/metric/{id}` | 单条计数（点击数实时刷新用） |
| GET | `/api/v1/categories` | 分类 schema（6 大分类 + 每类扩展字段 + `pan_types` 网盘类型列表） |

---

## 八、公开主页与静态内页

- **主页 `/`**：资料卡片墙，顶部有「热度 / 点击 / 最新 / 评分」排序切换与 **6 大分类筛选**；卡片显示评分、点击数，**填了下载链接的卡片右上角会显示「可下载」角标**。
- **静态内页 `/uisc/{id}.html`**：由 `core/StaticGen.php` 生成，Nginx 当静态文件直接返回。按来源/类型差异化展示（影视→演职人员、游戏→平台/厂商、动漫→话数/制作公司、手动项→分类扩展字段），简介下方展示 **下载地址块**（**仅当填写了链接时才显示**）。

> **API 调用次数属于后台内部指标**，仅后台可见；前台只展示评分 / 年份 / 点击。

- **生成时机**：每次同步（`cron/sync_trending.php` / `sync_refresh.php`）结束自动全量重建；后台「重新生成静态页」全量重建；**后台编辑/新增某条后自动只重建该条**。
- `uisc/` 目录需可写（生成器会自动建目录）。
- **顶部导航 / 前台模板**（v1.6.0 起）：导航入口、自定义外链、经典/简约模板、榜单有图无图、首页「最新更新」区块，全部在 **后台 →「站点设置 → 扫描+跳转」** 下半部分配置，改完刷新前台立即生效（无需重新生成静态页——这些是运行时渲染的）。
- **页脚**：底部介绍 / 声明 / 版权三段由 **后台 →「站点设置 → 基础设置」** 控制。

---

## 九、采集（同步）与定时计划任务

采集的全部行为集中在 **`core/Sync.php`** 一处：网页后台「同步采集」页的按钮、宝塔计划任务里的命令，**走的是同一套逻辑、同一套参数**。

### 9.1 一次采集做什么

1. **只取「最热」与「最多人看」**（不盲目翻页把站点灌满）：

   | 数据源 | 「最热」口径 | 「最多人看」口径 |
   |---|---|---|
   | TMDB（影视） | `/trending/all/{day\|week}` 官方趋势榜 | `/movie/popular` + `/tv/popular` + 综艺 `discover/tv?with_genres=10764` |
   | RAWG（游戏） | `/games?ordering=-added` 最多人添加 | `/games?ordering=-rating` 评分人气最高 |
   | 红果短剧（国产短剧） | `/calendar` 当日在播 | `/v0/subjects?type=2&sort=rank` 排行榜（按评分人数排序） |

2. **按各源官方要求限流**（这就是"添加限制，对比源站要求"的落地）：

   | 数据源 | 官方硬性要求 | 本系统自设的更保守限制 |
   |---|---|---|
   | TMDB | 限流约 50 请求/秒；免费 Key 需署名；单页 20 条 | **300 ms/请求**；每源每次 ≤ 20 条 |
   | RAWG | 免费 20,000 请求/月（≈27/小时）；单页 ≤ 40 条 | **1500 ms/请求**；每源每次 ≤ 20 条 |
   | 红果短剧 | 需配置 Base URL，无官方速率限制 | **200 ms/请求**；建议每次 ≤ 50 条 |

   另外还有两个硬闸：**单次采集的上游请求总数上限**（默认 8 次）、**每源每次入库条数上限**（默认 20 条）。

3. **重复就跳过**：同源同 ID 已存在 → 直接跳过；开启「同名同年」后，**跨数据源**的同名作品也只保留先采到的那一条。
4. **只给新增条目生成静态页**（不做全量重建，所以每小时跑也无压力）。
5. 每次结果都写进 `sync_log` 表，后台「同步采集 → 最近采集记录」与「同步日志」都能查。

### 9.2 参数在哪改

**后台 →「同步采集」→ 采集参数**（存 `app_settings` 表，即时生效、升级覆盖不丢）：

| 参数 | 默认 | 作用 |
|---|---|---|
| 每个源、每次最多入库多少条 | 20 | 单次采集量上限（TMDB 单页上限 20，RAWG 40） |
| 每个源最多翻几页 | 1 | 翻页越多越费上游配额 |
| 单次同步的上游请求总数硬闸 | 8 | 防止一次采集跑太久被网关掐断 |
| TMDB 热门榜的时间窗口 | 本周 | 每小时跑建议改「今日」 |
| 按什么口径取内容 | 两者都取 | 最热 / 最多人看 / 两者都要（自动去重） |
| 投票数低于此值的条目不要 | 0 | 「最多人看」的量化门槛，填 100 可滤掉冷门 |
| 遇到已采集过的条目怎么办 | 跳过 | 跳过最省配额；也可以选「刷新数据」 |
| 同名同年（跨数据源）是否也跳过 | 跳过 | 避免站内出现两遍同一部作品 |
| 两次自动同步的最小间隔 | 55 分钟 | 防手动连点 / 计划任务撞车 |
| 同步后顺手生成新增条目的静态页 | 是 | 只生成新增那几条 |

### 9.3 每小时采集（宝塔计划任务，推荐）

宝塔 → **计划任务** → 添加任务：

| 项 | 填什么 |
|---|---|
| 任务类型 | **Shell 脚本** |
| 任务名称 | 媒资库-每小时采集 |
| 执行周期 | **N 小时 → 1 小时**（或「每小时」） |
| 脚本内容 | `php /www/wwwroot/你的站点/cron/sync_hourly.php` |

> 命令里的 `php` 建议写绝对路径（如 `/www/server/php/72/bin/php`），避免计划任务环境里 PATH 不同导致找不到命令。
> **后台「同步采集」页会直接给出这条命令，带一键复制**（会自动用当前 PHP 版本对应的路径）。

这样配置后：

- **每小时**自动采集一次「今日最热 + 最多人看」，已存在的一律跳过，新增的顺手生成静态页。
- 脚本自己还有三重保护：① 距上次采集不足「最小间隔」会**自己跳过**（所以你多配几条计划任务也不会重复请求上游）；② 同一时刻只允许一个采集在跑（`config/sync.lock` 进程锁）；③ 请求总数不超上限。
- 用**网址**访问 `cron/sync_hourly.php` 会返回 **404**（脚本只允许命令行执行），别人无法远程触发采集烧你的上游配额。

### 9.4 其它两个计划任务（可选）

| 脚本 | 建议周期 | 作用 |
|---|---|---|
| `cron/sync_trending.php` | 每天一次 | 本周窗口、每源 40 条，**末尾全量重建静态页**，把整站对齐一次 |
| `cron/sync_refresh.php` | 每天一次 | 把超过 `cache.stale_days` 的老条目重新拉一遍（保持详情新鲜） |

### 9.5 命令行参数（排错用）

```bash
php cron/sync_hourly.php --help          # 看全部参数
php cron/sync_hourly.php --dry           # 只探测接口，不入库（排错首选）
php cron/sync_hourly.php --force         # 忽略「最小间隔」强制跑一次
php cron/sync_hourly.php --limit=10 --order=hot --sources=tmdb,hongguoduanju
php cron/sync_hourly.php --quiet         # 只输出一行结果，适合计划任务日志
```

退出码：`0` = 成功（含"因间隔跳过"）；`1` = 失败（上游全挂 / 配置错），宝塔计划任务日志里能直接看出成败。

---

## 十、后台

`https://你的域名/admin` → 输入后台令牌（后台「系统设置」里设的那个；未改过就是 `config.php` 的 `admin.token`）：

- **概览**：各源 / 各类型条目数。
- **条目管理（按 6 大分类建栏目）**：分类标签筛选；列表显示封面、分类、来源、**下载链接状态（✓ 有效 / ✗ 失效 / 未检测）**。
  - **链接失效检查**：每行「查」检测该条目的全部下载链接；工具栏「检查全部链接」批量滚动检测。结果记入 `link_checks` 表，列表与编辑弹窗的状态标随之更新。采用 HEAD（不支持时回退 GET），带超时。
  - **新增项目**：选分类 → 表单自动出现该类专属字段（如短剧=集数/导演/地区，游戏=平台/开发商）→ 填通用信息 + 下载链接（可留空）→ 保存即入库并生成静态页。
  - **下载链接（多条网盘）**：「+ 添加下载链接」可增删多行，每行 = 网盘类型 + 链接。类型在 **后台 →「系统设置」** 里增删（默认 **夸克 / 迅雷 / 光鸭 / 百度 / UC**）。
  - **编辑/删除**：所有字段都能改，保存后自动重建该条静态内页。
- **同步采集**：把「一次采集怎么跑」完整摊开给你看 —— 采集参数（可改）、各源官方限制对照表、**每小时计划任务的一键复制命令**、最近采集记录。按钮：`立即采集` / `强制采集（忽略最小间隔）` / `预演（只探测不入库）`。见第九节。
- **系统设置**：令牌、API Key、数据源、网盘类型 —— 见第十一节。
- **安全防护**：一眼看清安装器当前状态（未上传 / 已自锁 / 尚未安装因而可用）、敏感目录与内部文件的守卫是否到位，并对 `/install.php`、`/sql/install.sql` 做公网可达性实测；带「打开环境自检报告」按钮（自动带上当前令牌）。
- 「同步热门」（条目管理工具栏）：等于「同步采集 → 立即采集」，执行期间按钮会显示进度并在下方列出**每个源、每个接口**抓到了多少条、新增几条、跳过几条。
- 「重新生成静态页」：全量重建 `/uisc/*.html`。

> 新增分类或调整某类字段，只改 `core/Categories.php` 一处，后台表单与内页渲染自动跟随。

---

## 十一、系统设置（令牌 / API Key / 数据源 / 网盘类型）

**后台 →「系统设置」**，5 项都能在这里随时增删改，**不用再动 `config/config.php`**：

| 设置项 | 说明 |
|---|---|
| 后台管理令牌 admin.token | 登录后台用。旁边有「随机生成」；改完点保存，本页会自动切到新令牌。 |
| TMDB API Key | 影视 / 短剧 / 电视剧采集用。旁边有「测试连通」，直接告诉你 Key 是否可用。 |
| RAWG API Key | 游戏采集用。同样可「测试连通」。 |
| 启用哪些数据源 | TMDB / RAWG / 红果短剧 勾选（至少一个）。决定「同步热门」跑哪些源。 |
| 网盘类型 | 点「添加」逐个加（如 `123网盘`），点 chip 上的 `×` 删除；「恢复默认」一键回到夸克/迅雷/光鸭/百度/UC。 |

### 存哪里、优先级怎么算

- 保存到数据库 **`app_settings`** 表（首次打开该页若表不存在会**自动建表**，无需手工导 SQL）。
- 优先级：**后台保存的值 > `config/config.php`**。
- **留空 = 不覆盖**：某项清空后保存，就自动回退到 `config.php` 里的值。
- 点「全部恢复为 config.php 的值」可一次性清掉所有后台覆盖。

### 为什么这样设计

- 整包覆盖升级源码时，**后台改过的设置不会被冲掉**（存在数据库里，不在文件里）。
- `config/config.php` 可以是只读的（更安全），也能改这些设置。
- 忘了令牌怎么办：`config/config.php` 里的旧令牌**仍然有效**（自检页也接受它）；想彻底回退就导入一次 `sql/migrate_settings.sql` 里的表结构后 `DELETE FROM app_settings`，或直接删表。

**相关接口**（后台已封装，自研前端可用）：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v1/admin/settings` | 读取当前设置（含「生效值 / 后台覆盖值 / 配置文件里的值」） |
| POST | `/api/v1/admin/settings` | 保存设置 |
| POST | `/api/v1/admin/settings_reset` | 传 `{key}` 清单项、`{all:1}` 清全部覆盖 |
| POST | `/api/v1/admin/settings_test` | 传 `{kind:"tmdb"/"rawg"/"hongguoduanju", key?}` 探测上游是否可用 |
| GET | `/api/v1/admin/security` | 读取安全防护状态（安装器是否已移除、目录拒访、守卫覆盖率），后台「安全防护」页用 |
| GET | `/api/v1/admin/sync_plan` | 采集参数 + 各源官方限制对照 + 最近采集记录 + 计划任务命令（后台「同步采集」页用） |
| POST | `/api/v1/admin/sync` | 执行一次采集。body 可带 `{force:1, dry:1, limit, window, order, sources, build}` |

> 采集参数虽然不在「系统设置」页里，但**保存到同一张 `app_settings` 表**，走的也是 `POST /api/v1/admin/settings`（键名 `sync_*`）。

---

## 十二、扩展新数据源

1. 在 `adapters/` 新建 `Xxx.php` 实现 `Adapters\Adapter` 接口：`normalize()` 输出统一结构，`trendingFetch($ep, $limit, $order)` 负责拉某一个具体接口并归一化。
2. 在 **`core/Sync.php` 的 `profiles()` 里登记**该源的：接口文档地址、官方配额说明、单页上限 `page_size`、自设的请求间隔 `delay_ms`、是否需要 Key、以及「最热 / 最多人看」两个口径对应的接口；同时在 `endpointsFor()` 里补上它的接口清单（`/api/v1/admin/sync_plan` 会自动把它渲染到后台对照表里）。
3. 在**后台 →「系统设置」→ 启用哪些数据源**里勾上它（内部接口已内置白名单，新增源需同时补 `api/unified.php` 的 `$map` 与 `Settings::schema()` 的 `options`）。
4. 在 `adapters/` 对应文件的 `endpointList()`（如有）与 `api/unified.php` 的适配器映射里登记类名。

无需改动路由与前端。

---

## 十三、环境自检（check.php）

部署后打开 `https://你的域名/check.php?token=你的后台令牌`（等价入口 `/check`、`/health`），一页看完所有部署是否就绪。

> 🔒 **v1.4.0 起鉴权更严**：未通过令牌校验的请求**一律返回 404**，不再显示"需要令牌"的输入框 —— 扫描器无法确认这个文件的存在，也无法拿它爆破令牌。
> 最省事的打开方式是 **后台 →「安全防护」→「打开环境自检报告」**（自动带上当前令牌）。
> 站点还没设置任何令牌时（刚装完），只允许 `127.0.0.1` / 内网地址访问。

| 地址 | 作用 |
|---|---|
| `/check.php?token=xxx` | HTML 报告（健康度评分 + 逐项修复建议） |
| `/check.php?format=json&token=xxx` | 机器可读 JSON（便于脚本 / 监控 / CI，`success` 即总体结论） |
| `/check.php?write=1&token=xxx` | 额外做一次真实写库测试（插入一条临时数据后立即删除） |
| `/check.php?nohttp=1&token=xxx` | 跳过网络类检测，速度最快 |

**11 组检查**：① 运行环境 ② 文件与目录权限（真实写入探针 + 核心文件完整性）③ 配置项（**安装状态**、口令/令牌是否仍是默认占位、**后台设置覆盖了哪几项**）④ 数据库与数据（表结构含 `app_settings`、缺列直接给 `ALTER` 补救 SQL、分类分布、静态页覆盖率）⑤ 路由 / 伪静态 ⑥ 上游连通性 ⑦ 代码语法（有 `exec` 时用 `php -l` 逐个校验）⑧ **安全防护**（安装器是否已移除、目录拒访文件与内部文件守卫覆盖率、敏感文件的公网可达性实测）⑨ **采集与计划任务**（启用的数据源、该源 Key 是否齐全、采集参数摘要、`cron/` 脚本是否就位、最近一次采集的时间与成败、同步锁是否残留、可直接复制的计划任务命令）⑩ **站点设置 / SEO / 网盘接口**（v1.6.0，含 `/robots.txt` 与 `/sitemap.xml` 真实探测、PanSou 连通性）⑪ **注册与资源请求**（v1.6.2，含两张新表结构、`password_hash` 可用性、**会话目录可写性**、`/user/register` 与 `/request` 的真实 HTTP 探测）。

> 报告里数据库口令、API key、后台令牌全部自动打码；**后台改过的令牌和 `config.php` 里的旧令牌都能进**（防止改完令牌把自己锁在外面）。
> **全部确认无误后请删除或重命名 `check.php`**，彻底消除暴露面。

---

## 十四、PHP 版本兼容性

---

## 十五、站点设置（基础设置 / SEO / 扫描跳转 / 网盘链接 / 接口配置 / 资源请求）

> **v1.6.0 新增，v1.6.2 增加第 6 个页签。** 后台左侧「站点设置」下有 **6 个**二级页面，全部**即时生效**，且值存在数据库 `app_settings` 表里 —— 整包覆盖升级也不会丢。
> 优先级依旧是：**后台保存的值 > `config/config.php` 的 `site|seo|redirect|pan|pansou|req` 段 > 内置默认**；后台留空 = 用文件的值。

### 15.1 基础设置

| 项 | 作用 | 说明 |
|---|---|---|
| 网站名称 / 隐藏名称 / 宣传语 | 顶栏、浏览器标题、页脚 | 勾「隐藏名称」只在**已设置 LOGO** 时才生效，否则顶栏会空白 |
| 网站 LOGO / 浏览器 icon | 顶栏图形、标签页小图标 | 可**直接上传**（存到 `/uisc-assets/`，随机改名、单张 2MB 内），也可填图片网址 |
| 网站网址 | canonical / `robots.txt` / `sitemap.xml` 的绝对地址 | 例 `https://cs.zyfx.wang`，**结尾不要带 `/`** |
| 底部介绍 / 声明 / 版权 | 页脚三段 | 支持换行；备案号建议写在版权里 |

### 15.2 SEO

- **首页三件套**：`SEO 标题` / `关键词` / `描述` → 首页 `<title>`、`meta keywords`、`meta description`。
- **标题模板**（页面内有占位符说明，改完可点「预览」看效果）：
  - 列表页 / 分类页：`{site}` `{type}` `{sort}` `{page}`
  - 内页：`{title}` `{year}` `{type}` `{site}` `{region}`
- **统计代码**：整段粘贴（51.la / 百度统计等），会原样插到每个页面 `</head>` 前。
- **内页描述取简介前多少字**：`0` = 不截取，直接用上面的 SEO 描述。
- **`robots.txt` / `sitemap.xml`（v1.6.0 起由路由动态输出）**：
  - `https://你的域名/robots.txt` —— 后台「robots.txt 内容」留空则用默认规则（允许首页与内页，屏蔽 `/admin`、`/api/`、`/3/`、`/track`、`/check`）。
  - `https://你的域名/sitemap.xml` —— 按已入库条目动态生成，可设最多收录条数；关掉则返回非 sitemap 内容。

> ⚠️ 这两个地址依赖伪静态（见第四节）。如果站点根目录里还放着**物理的** `robots.txt` 文件，Nginx 会优先返回那个文件，动态路由不会生效。

### 15.3 扫描 + 跳转（PC 端访问方式）

**三种模式**：

| 模式 | 访客看到什么 |
|---|---|
| `跳转 + 扫码` | 先展示二维码 + 提示语，约 3 秒后自动跳到目标地址；页面上还有「点这里直接访问」兜底链接 |
| `仅跳转` | 不展示二维码，约 1 秒后直接跳走 |
| `仅扫码` | 只展示二维码，不跳转 |

- **手机端是否同样处理**：默认 **否** —— 只拦 PC 访客，手机用户直接进站（体验更好）。选「是」则手机也按上面的方式处理。
- **降级规则**（填不全也不会把站点锁死）：
  - 跳转地址与二维码**都没填** → 不拦截任何人（等于关闭）。
  - 只填了二维码、模式是「跳转 + 扫码」 → 自动降级为「仅扫码」。
  - 模式是「仅扫码」但没传二维码 → 不拦截。
- **只拦首页入口**：内页 `/uisc/{id}.html` 是已生成的静态文件，不会被拦 —— 否则搜索引擎收录会受影响。
- 同一页下半部分是 **前端模板与顶部导航**：
  - `前台模板`：**经典**（卡片墙）/ **简约**（更紧凑，默认带明暗两套配色）。
  - `顶部导航显示`：勾选要出现在顶栏的入口（首页 / 全网榜单 / Top250 / 游戏排行 / 最新更新 / 联系我们）。**全部不勾 = 顶栏不显示导航**。
  - `顶部其他外链`：一行一个，格式 `链接文字|网址`，会以新窗口打开。
  - `全网榜单样式`：**有图**（卡片网格）/ **无图**（纯文字行，加载更快）。
  - `最新列表`：是否在首页显示「最新更新」区块。

### 15.4 网盘链接（网盘账号）

按网盘分组维护三件套：

| 字段 | 用途 |
|---|---|
| `Cookie` | 从浏览器登录该网盘后整串复制。**本系统只负责保存与检测，不会主动登录你的网盘账号。** |
| `默认转存目录` | 例 `/资源/电影`；留空表示网盘根目录 |
| `临时资源目录` | 转存过程中的中转目录，处理完可清理；留空则与默认转存目录相同 |

- 顶部「要管理哪些网盘」决定下面出现哪几个页签（默认 6 种：夸克 / 阿里 / 百度 / UC / 迅雷 / 光鸭）。
- 新增一种网盘只需改 `core/Settings.php` 的 `panAccounts()`，表单与页签会自动出现。

### 15.5 接口配置（PanSou 网盘搜索）+ 采集后自动补下载地址

[PanSou](https://github.com/fish2018/pansou) 是开源的网盘资源聚合搜索服务（聚合 TG 频道与网盘插件），默认监听 **8888** 端口。

**第 1 步：把 PanSou 跑起来**（与站点同一台服务器时最省事）

```bash
# 示意：具体以 PanSou 官方文档为准
docker run -d --name pansou -p 8888:8888 ghcr.io/fish2018/pansou:latest
```

装好后先自测一下：

```bash
curl http://127.0.0.1:8888/api/health
```

**第 2 步：后台填配置**

| 项 | 怎么填 |
|---|---|
| 启用 PanSou 接口 | 选「启用」 |
| 部署方式 | 与站点同机选「本机部署」 |
| 接口地址 | **只填服务根**：`http://127.0.0.1:8888`（**不要**带 `/api/search`） |
| 账号 / 密码 | 只有服务端开了鉴权才需要 |
| 启用哪些网盘类型 | 只勾站点真正会展示的几种，能显著减少无用的补地址请求 |
| 采集后自动补网盘下载地址 | 选「开启」 |

填完点 **「测试连接」**（会请求一次 `/api/health`，回报频道数 / 插件数 / 版本）。

**第 3 步：写入策略（重要）**

| 策略 | 行为 | 适用 |
|---|---|---|
| `仅标题高度匹配才自动写入`（默认，推荐） | 相似度 ≥ `最低相似度` 才写进下载地址，其余进「待确认」 | 大多数站点 |
| `只要搜到就自动写入` | 命中即写 | 内容单一、命名规范的小站 |
| `一律只进「待确认」队列` | 全部人工过一遍 | 对链接准确性要求极高时 |

- **最低相似度**：建议 `75~85`。算法是 `similar_text` 相似度 + **包含关系加分**（资源标题通常带「1080P / 全集 / 更新至xx」后缀）+ **年份命中加分**。
- **每条资源最多写入几个网盘链接**：每种网盘取最优的一条，超出则按分数截断。建议 `3~5`。
- **两次搜索的最小间隔**：PanSou 会去搜 TG 频道和第三方插件，连续请求容易被限流，建议 `800~1500` 毫秒。
- **搜索结果缓存时长**：同一关键词在此时长内不重复请求。建议 `720`（12 小时）。

**第 4 步：什么时候会被触发？**

1. **采集时自动补**：`同步采集` 跑完、新增条目入库并生成静态页之后，会自动拿标题去搜一遍；**补到地址的条目会再生成一次静态页**（否则前台看不到下载按钮）。
   - 中文标题搜不到高分时，会自动用 `original_title`（原始外文标题）再搜一次。
   - 报告在后台「同步采集 → 最近采集记录」的 `pan` 字段里（检查 / 写入 / 进队列 / 无匹配 / 用时）。
2. **后台手动补**：接口配置页的「试搜」（只搜索、不写库）与「立即补地址」（给还没下载地址的条目补）。
3. **待确认队列**：「待确认」页签里可逐条 **采纳**（写入下载地址并出队）或 **丢弃**；后台顶栏会显示待确认条数。

**写入规则（防止污染站长手工数据）**

- 按 **网盘标签去重**：某个网盘在条目里已有链接时，**保留站长手工填的那条**，不会被覆盖。
- 下载地址统一存成 JSON：`[{"label":"夸克网盘下载","url":"https://..."}]`；同时也兼容旧版「一行一个地址」的纯文本，读取时自动识别。
- 带提取码的链接会自动拼成 `?pwd=xxxx`。

**表格（自动创建，无需手工导 SQL）**

| 表 | 用途 |
|---|---|
| `pan_search_cache` | 搜索结果缓存，后台「清空搜索缓存」会清它 |
| `pan_pending` | 「待确认」队列（同一条目只保留最新一次搜索结果） |

> 若数据库用户没有 `CREATE` 权限，可手工导入 `sql/migrate_site_seo.sql`（全是 `CREATE TABLE IF NOT EXISTS`，不改动既有表）。

**自检**：`check.php` 第 10 组「站点设置 / SEO / 网盘接口」会逐项检查站点信息是否填写、SEO 是否完整、`/robots.txt` 与 `/sitemap.xml` 是否真能访问、`uisc-assets/` 是否可写、跳转扫码配置是否自相矛盾、PanSou 是否连通、待确认队列有多少条。

---

## 十六、响应式与多端适配

> **v1.6.1 新增。** 前台主页与后台管理用同一套代码同时适配手机 / 平板 / 大屏，**无需任何配置**。

### 1) 断点一览

| 断点 | 面向 | 前台做了什么 | 后台做了什么 |
|---|---|---|---|
| `>=1800px` | 超宽屏 | 容器放宽到 1480px | — |
| `>=1400px` | 大屏 | 容器 1320px、卡片放大 | 容器 1320px、统计卡放大 |
| `<=1024px` | 平板横屏 | 上下边距收紧、hero 留白收窄 | 边距收紧、统计卡变小 |
| `<=768px` | 平板竖屏 | 网格 152px、详情区间距收窄 | **Tab / 分类胶囊横滑**、**表格横滑且首列粘住** |
| `<=560px` | 手机 | 顶栏折行、工具栏横滑、下载地址竖排 | **弹层整屏**、工具条两列、设置项单列 |
| `<=380px` | 小屏手机 | 卡片 126px、字号整体降级 | 统计卡与工具条进一步压缩 |
| 横屏 + `max-height:520px` | 手机横屏 | 首屏压缩、顶栏取消吸附 | 弹层从整屏回到居中卡片 |

### 2) 前台（`core/Views.php`）

- 容器宽度由 CSS 变量 `--maxw` 控制、左右内边距由 `--pad` 控制 —— 想统一调整留白，改这两个变量即可。
- 手机端顶栏把**搜索框折到第二行铺满**；顶部导航与排序 / 筛选工具栏**横向滑动**，触摸目标加高到 38px 以上。
- 卡片网格最小宽度随屏宽降到 152 / 140 / 126px，**320px 的老机型也能并排两张卡**。
- 内页海报 184 → 124 → 104px；「下载地址」在手机端改为**网盘标签与地址竖排**，长链接不会被挤成两三个字一行。
- 「最新更新」区块在手机端标题独占一行（最多两行截断），分类与年份折到下一行。

### 3) 多端能力一览

| 能力 | 说明 | 需要配置吗 |
|---|---|---|
| **跟随系统深色** | 后台「站点设置 → 扫描+跳转 → 前台明暗模式」，默认「跟随系统」；**经典与简约模板都支持** | 想固定亮色就选「亮色」 |
| **刘海屏 / 底部横条** | `viewport-fit=cover` + `env(safe-area-inset-*)`，左右留白与页脚底部自动避让 | 自动 |
| **触屏去粘滞** | `@media (hover:none)` 下取消卡片位移与 hover 高亮（手机上点完不会「卡住浮起」） | 自动 |
| **减少动画** | 系统开启「减少动态效果」后，过渡与位移自动关闭 | 自动 |
| **高对比度** | 系统开启高对比度后，文字与边框自动加深 | 自动 |
| **打印排版** | `Ctrl+P` 时隐藏顶栏 / 导航 / 首屏 / 工具栏 / 分页 / 页脚，内页 hero 改黑字白底 | 自动 |
| **手机不放大** | 搜索框与后台输入框在小屏用 16px 字号，iOS 聚焦时不会整页放大 | 自动 |
| **键盘可达性** | 所有可点元素统一 `:focus-visible` 描边（顶栏 / 导航用白色描边） | 自动 |

### 4) 后台（`admin/index.php`）

- 一级 Tab（概览 / 条目管理 / 同步采集 / 系统设置 / 站点设置 / 安全防护 / 同步日志）、站点设置下的二级 Tab、条目的分类胶囊 —— 窄屏一律**横向滑动**，不再换行挤压。
- 条目表格包在横向滚动容器里（`table{min-width:680px}`），滑动查看时**首列（封面）粘住**，滑到右边也能对上号。
- 「编辑条目」弹层在手机上**整屏显示**（去圆角、占满高度、底部避开安全区），软键盘不会把表单顶出屏幕；**手机横屏**时自动变回居中卡片。
- 工具条（搜索 + 6 个按钮）在手机上改为**两列网格**，搜索框独占一行。
- **明暗主题**：顶栏右上角按钮循环「跟随系统 → 亮色 → 暗色」，选择存在浏览器本地（`ml_admin_theme`）。进入页面时先定好主题，**不会亮色闪一下再变暗**；处于「跟随系统」时还会随系统偏好变化自动跟随。
- 原先 9 处内联颜色的小提示条统一改成类名 `.note-ok / .note-warn / .note-err`，暗色下才能正常显示。

### 5) 怎么确认生效

1. 打开 `check.php`，看 **10.6b「响应式自适应」** —— 会显示前后台断点数量与已启用的多端能力（断点为 0 = 文件还是旧版）。
2. 把浏览器窗口拉到 375px 左右（或直接用手机访问），确认：顶栏搜索框折行、导航可横滑、卡片两列、内页下载地址竖排。
3. 打开后台，点右上角切到暗色，确认表格、弹层、提示条、状态标都正常。
4. 想在本地一次看到三档并排的真实效果：打开源码里的 `preview/responsive.html`（预览页不随发布包发布）。

---

## 十七、注册与资源请求（v1.6.2 新增）

访客**注册账号并登录后**，可以在前台「**资源请求**」页填写想找、但站内还没收录的资源；站长在后台「**资源请求**」页逐条处理（改状态、写回复、删除），**回复只有提交者本人能看到**。

### 17.1 三个前台页面

| 地址 | 谁能看 | 说明 |
|---|---|---|
| `/user/register` | 所有人（站长可关） | 注册：用户名 + 密码 + 选填邮箱；默认带一道算术验证码；注册成功直接登录 |
| `/user/login` | 所有人 | 登录；退出是 `/user/logout` |
| `/request` | 列表按设置开放，**提交要求登录** | 资源请求页：提交表单 + 「我的请求」 + 「大家都在找」列表 |

顶栏右侧会自动出现「登录 / 注册」（已登录则显示「我的请求 · 用户名 / 退出」），顶部导航也会多一个「资源请求」入口 —— 两处都能在后台关掉。

### 17.2 后台两个地方

**① 一级 Tab「资源请求」**（处理数据）

- 顶部状态条：注册用户 / 待审核 / 请求总数 / 待处理 / 今日新增，另有「前台页面 ↗」直达按钮
- **请求列表**：按状态筛选、按名称或说明搜索；每条可改状态（待处理 / 已找到 / 暂无资源 / 已回复 / 已关闭）、写回复、删除
- **注册用户**：搜索用户名 / 邮箱；支持「通过/启用」「禁用」「重置密码」「删除」；**删用户会保留他提交过的请求**（记录里仍有用户名）

**② 站点设置 → 资源请求**（改行为）

| 项 | 默认 | 说明 |
|---|---|---|
| 是否开放注册 | 开放 | 关掉后只有已有账号能登录 |
| 提交是否要求登录 | 必须登录 | 允许游客提交就改成「游客也能提交」 |
| 注册是否需要审核 | 免审核 | 选「需审核」后新账号为**待审核**，要你在后台点「通过/启用」 |
| 注册是否使用算术验证码 | 开 | 一道加减法，**不依赖 GD 图形库**，没装 GD 也能跑 |
| 未登录访客能否浏览请求列表 | 允许 | 联系方式与站长回复始终只有本人可见 |
| 请求页是否展示「大家都在找」 | 显示 | 关掉后只剩提交表单与自己的请求 |
| 是否收集联系方式 | 收集 | 默认用注册邮箱预填；关掉表单更短 |
| 顶部导航显示「资源请求」入口 | 显示 | 与「顶部导航显示」不冲突，不会重复出现 |
| 每人每天最多提交几条 | 5 | 0 = 不限制；建议 3~10 |
| 资源名称最少字数 | 2 | 挡住「1」「求」这类无效提交 |
| 页面名称 / 页面说明 / 页面公告 | 资源请求 / 空 / 空 | 页面大标题、标题下说明、提交框上方的公告条 |

### 17.3 建表：**不用手工导 SQL**

两张表第一次用到时自动创建：

| 表 | 存什么 |
|---|---|
| `app_users` | 用户名、邮箱、`password_hash` 散列、状态（正常/禁用/待审核）、连续失败次数与锁定时间、注册 IP、提交数、注册与最后登录时间 |
| `app_requests` | 提交人、资源名称、类型、年份、补充说明、联系方式、状态、**站长回复**、命中的站内条目 id、提交 IP、时间 |

> 覆盖升级后即使表已存在、只是少了几列，程序也会**自动 `ALTER` 补齐**（`SHOW COLUMNS` 比对后逐列加）。
> 只有在「数据库用户没有建表 / ALTER 权限」时，才需要手工导入 `sql/migrate_users.sql`，或到 宝塔 →「数据库 → 该库用户 → 勾选所有权限」。

### 17.4 自带的风控（不用另外配）

| 风险 | 处理 |
|---|---|
| 机器人批量注册 | 算术验证码（可关）+ 蜜罐隐藏字段 + **同 IP 一小时最多注册 3 个** |
| 撞库 / 暴力登录 | 连续错 5 次锁定 15 分钟；用户名不存在时也走一次散列运算，**响应快慢不泄露账号是否存在** |
| 提交刷屏 | 每人每日上限（默认 5）+ **同 IP 十分钟最多 15 条** + 同一人重复提交同名请求会被挡下 |
| 表单被跨站提交 | 所有写操作都校验 CSRF 令牌（会话内随机串，恒时比较） |
| 会话被劫持 | 登录成功即 `session_regenerate_id`；cookie 为 `HttpOnly`，HTTPS 下自动加 `Secure` |
| XSS | 输出统一走 `htmlspecialchars`；`/request?probe=` 的即时提示由**服务端先转义**再交给前端 |
| 密码泄露 | 只存 `password_hash()` 散列，绝无明文；数据库被拖走也无法直接还原 |

### 17.5 顺手做的前台查重

在「资源请求」页输入资源名时会自动在站内搜一次（`GET /request?probe=关键词`），
已有同名条目就**直接给出内页链接** —— 既省了用户一次搜索，也提醒他「其实站里有」。
这个请求失败会静默隐藏，不影响提交。

### 17.6 自检怎么看

`check.php` 的 **第 11 组「注册与资源请求」** 会检查：
注册开关与风控设置摘要 · 两张表是否存在、关键字段是否齐全（缺列给提示）· 用户与请求的数量概况（待审核 / 待处理会标黄）·
`password_hash` 是否可用 · **会话目录是否可写**（这是宝塔上「登录成功后又变回未登录」的头号原因）·
`core/User.php` / `core/UserViews.php` / `user.php` 是否就位、`index.php` 是否挂上了路由 · **真实 HTTP 探测 `/user/register` 与 `/request`**（`?nohttp=1` 跳过）。

---

## 十八、常见问题

- **Parse error: unexpected '=>' / '??' 之类的语法错误**：说明站点 PHP 版本与代码不匹配。v3.1.1 起仅支持 PHP 8.2+，请确认站点 PHP 版本 ≥ 8.2 并覆盖上传了**全部**文件（尤其 `core/`、`api/`），并清一次 opcache / 重启 PHP。
- **访问 `/admin` 或 `/api/v1/*` 返回 404**：伪静态没配。见第四节，`location / { try_files $uri $uri/ /index.php?$query_string; }`。
- **502 / 空白**：检查 PHP 是否开启 `curl`、`pdo_mysql`；看站点错误日志。
- **返回 401**：后台接口需带 `X-Admin-Token: 你的token`。
- **数据库连接失败**：多为主机/端口/密码不对，或该用户未授权到这个库；MySQL 8 + 老 PHP 见第一节的认证提示。
- **上游 429**：已内置指数退避；降低同步频率或批量大小。
- **图片不显示**：确认用的是 TMDB 图片规则（`image.tmdb.org/t/p/`），本服务默认透传原始图片路径。
- **部署自检报红不知道从哪下手**：打开 `/check.php`，每个「失败」项都附了具体修复步骤。
- **访问 `/install.php` 返回 404？** 两种可能：① **站点已经装好了** —— 这是**正常现象**，安装器已自锁（`config/install.lock` 存在即对所有人 404），与文件不存在完全一样；确实要重装就先删掉 `config/install.lock` 与 `config/config.php`。② **站点根目录里没有 `install.php`**（用的是旧包，或漏传了）—— 从发布包把它传到站点根即可，或改用终端 `php tools/install-cli.php`。
- **怎么重装 / 迁移到新服务器？** 整包传上去，在终端执行 `php tools/install-cli.php --force`，按提示填新库信息即可（原 `config.php` 会自动备份为 `config.php.bak-日期时间`）。
- **访问首页提示「站点尚未安装」**：这是**正常**引导页——说明还没装，或 `config/config.php` 里的 `db` 仍是示例值。页面上有一个「**🚀 开始网页安装**」按钮，点它按向导装即可（也可以按提示在终端跑 `php tools/install-cli.php`）。装完后提示自动消失，已安装站点不受影响。
- **包解压后发现没有 `config/config.php`**：正常。它由 CLI 安装器生成，或手动把 `config/config.sample.php` 复制一份改名即可。
- **升级后 `check.php` 打不开了（返回 404）**：v1.4.0 起自检页未带正确令牌一律 404。请用 `check.php?token=你的后台令牌`，或从 **后台 →「安全防护」→「打开环境自检报告」** 进入。
- **后台「安全防护」里 `install.php` 那行是什么含义？** 三种状态：**未上传**（提示 · 需要网页安装就从发布包补传 `install.php`）、**存在且已写入 install.lock**（已防护 · 对所有人 404，无需处理）、**存在但站点尚未安装**（提示 · 此刻它确实能被任何人打开安装向导 —— 按向导装完会自动失效；不打算用它就删掉站点根的这个文件）。
- **后台「系统设置」保存报错「设置表不可写」**：该数据库用户没有建表权限。宝塔 → 数据库 → 该库用户 → 勾「所有权限」；或在 phpMyAdmin 里导入一次 `sql/migrate_settings.sql`。
- **改了后台令牌后进不去后台了**：`config/config.php` 里的旧令牌**仍然可用**（自检页也认它）。用它登录后到「系统设置」里查看/重设新令牌即可。
- **在后台改了 API Key / 网盘类型，但没生效**：确认点了「保存设置」，然后刷新前台页面（静态内页是生成时写死的，改完设置后到「条目管理」点一次「生成静态页」全量重建即可）。
- **想让某项回到配置文件的值**：把该项留空后保存，或点「全部恢复为 config.php 的值」。
- **升级覆盖后设置丢了？** 不会。后台设置存在数据库 `app_settings` 表，不在文件里。
- **后台页面点查询/保存无反应，控制台报 `api/v1/...` 跨域或 404**：多为伪静态未生效（见第四节）；子目录部署也需保证 `try_files` 指向该目录下的 `index.php`。
- **点「同步热门」没反应（连提示都不弹）**：这是 v1.5.0 专门修掉的老毛病。以前后台用 `fetch().then(r=>r.json())`，一旦服务端返回的是 HTML（PHP 致命错误页 / 502 / 504 网关超时），`json()` 会解析失败并中断，页面就"毫无反应"（只在浏览器控制台留红字）。现在**任何非 JSON 响应都会转成一句人话提示**（含 HTTP 状态码与原因），按钮也会显示"正在采集…"直到出结果。若仍失败，直接照提示里的状态码查：`504` = 采集耗时被网关掐断（把「每源条数」「请求上限」调小）、`500` = PHP 致命错误（打开 `check.php` 看第 7 组）、`404` = 伪静态没配。
- **同步点了但"新增 0 条"**：三个最常见原因，按顺序排查 ——
  ① **该源没填 API Key**（TMDB / RAWG 必填）：`check.php` 第 9 组会直接标红"数据源配套密钥：已启用但缺少 XX Key"；也可以在后台「系统设置 → 测试连通」点一下。
  ② **条目其实都已存在**：本系统「重复就跳过」，第二次跑同一个榜单自然是 0 新增。报告里会写"重复跳过 N 条"，这是**正常**的。
  ③ **服务器访问不了境外接口**：看 `check.php` 第 6 组「上游数据源连通性」，或在后台点「预演（只探测不入库）」看每个接口的报错。
- **怎么确认每小时的计划任务真的在跑？** 三个地方能看：① 后台「同步采集 → 最近采集记录」会出现「计划任务」来源的行；② 宝塔 →「计划任务」→ 该任务的「日志」；③ `check.php` 第 9 组「最近一次采集」会显示 N 分钟前的结果。
- **多配了几条计划任务会不会把上游打爆 / 站内重复？** 不会。脚本有三重限制：距上次采集不足「最小间隔」（默认 55 分钟）会**自己跳过**、同一时刻只允许一个采集在跑（进程锁）、单次请求总数有硬闸（默认 8 次）。而且**已采集过的条目一律跳过**，不会重复入库。
- **想让站点多一点内容怎么办？** 三种组合：把「每个源、每次最多入库多少条」调到 40、把「每个源最多翻几页」调到 2~3、或把「按什么口径取内容」保持「两者都取」。注意 RAWG 免费额度是 20,000 次/月，别把「请求上限」调太大。
- **采集参数改了，网页按钮和计划任务都会变吗？** 会。两者读的是同一份参数（`app_settings` 表的 `sync_*` 键），改完保存即时生效，计划任务下次执行就用新参数。
- **`cron/sync_hourly.php` 用浏览器打开是 404，是不是文件没传上去？** 不是。该脚本**只允许命令行执行**（别人无法用网址触发采集），网页访问返回 404 是设计如此。用终端跑 `php cron/sync_hourly.php --help` 能出帮助就说明文件没问题。
- **上传 LOGO / 二维码报「无法创建 uisc-assets 目录」「写入失败」**：站点目录不可写。在宝塔 → 网站 → 该站点 → 目录里把 `uisc-assets`（不存在就新建）权限设为 `755`、属主 `www`；`check.php` 第 10 组会直接标出这一项。
- **改了 SEO / 模板 / 导航，前台没变化**：先强刷（`Ctrl+F5`）——这些是运行时渲染的，不需要重新生成静态页。若仍未变，确认点的是「保存」而不是只改了表单；静态内页的 `<head>` 也是运行时拼的，但若你开了页面缓存（宝塔「网站加速」/ CDN），需要清一次缓存。
- **`/robots.txt` 或 `/sitemap.xml` 打不开（404 / 返回首页 HTML）**：① 伪静态没配（见第四节）；② 站点根目录里放了**物理的** `robots.txt`，Nginx 优先返回它，动态路由不生效；③ 检查 `check.php` 第 10 组的「robots.txt / sitemap.xml 实际可访问」两行。
- **`sitemap.xml` 里条目很少或为空**：它是按已入库条目动态生成的，没内容时只有首页一条。另外注意「sitemap 最多收录条数」这项上限（默认 5000）。
- **配了「仅扫码」但手机直接进站了**：这是**默认行为**——「手机端是否同样处理」默认选「否」。要手机也拦，把它改成「是」。
- **配了跳转但访客没被拦**：三种可能，`check.php` 第 10 组会列出来：跳转地址与二维码都没填（= 关闭）、模式是「仅扫码」却没传二维码、访客是手机而「手机端是否同样处理」选了「否」。另外**只有首页入口会被拦**，直接访问内页 `/uisc/xx.html` 不会被拦（这是有意设计，避免影响收录）。
- **点「测试连接」报「连不上 PanSou」**：① PanSou 没启动（`curl http://127.0.0.1:8888/api/health` 自测）；② 地址写成了 `.../api/search`（**只能填服务根**）；③ 远程部署时安全组 / 防火墙没放行 8888；④ 服务器 PHP 没装 `curl` 扩展（会退回流式请求，但更容易超时）。
- **采集跑完了，但条目还是没有下载地址**：看后台「同步采集 → 最近采集记录」里的 `pan` 部分 —— `nomatch` 是「没搜到」、`queued` 是「进了待确认队列」、`written` 才是「已写入」。若 `nomatch` 很多，多半是条目本身太冷门，或「最低相似度」定得太高（可先降到 70 试）。
- **怎么确认自动补的地址没写错？** 默认策略就是为此设计的：只有标题相似度达阈值才自动写入，其余全部进「待确认」队列，你在后台逐条看一眼再采纳。写入时还会**按网盘去重**，不会覆盖你手工填的链接。
- **`pan_search_cache` / `pan_pending` 两张表要手工建吗？** 不用。`core/PanSou.php` 首次使用时自动 `CREATE TABLE IF NOT EXISTS`；数据库用户没有 `CREATE` 权限时，才需要手工导入 `sql/migrate_site_seo.sql`。
- **升级到 v1.6.0 要执行 SQL 吗？** 不需要。站点设置只是往已有的 `app_settings` 表写 key-value，**没有新增或改动任何字段**；PanSou 的两张表自动创建。（想显式对齐结构就导入一次 `sql/migrate_site_seo.sql`。）
- **手机 / 平板上打开站点，版式对不对？** v1.6.1 起前台与后台都已做响应式。若手机上仍是「挤成一团」，说明 `core/Views.php`、`admin/index.php` 还是旧版：`check.php` 的 **10.6b** 项会显示断点数量，前台应为 **14**、后台应为 **9**，为 0 就是没覆盖成功。
- **桌面端会不会被改丑？** 不会。前台断点集中在 1400px 以上与 1024px 以下，1280~1440 的常规笔记本几乎不受影响；后台同理。
- **访客的系统是深色，站点自动变暗了，能关掉吗？** 能。后台 →「站点设置 → 扫描+跳转 → 前台明暗模式」改成「**亮色**」并保存。选「暗色」则反过来固定成暗色；选「跟随系统」（**默认**）就按访客设备来。
- **后台右上角那个圆按钮是干什么的？** 明暗切换。点一下循环「跟随系统 → 亮色 → 暗色」，**只存在你自己浏览器里**，不影响访客、也不写数据库。
- **打印页面时把导航都打出来了？** v1.6.1 起 `Ctrl+P` 会自动隐藏顶栏 / 导航 / 工具栏 / 分页 / 页脚，打印出来是干净正文。若仍打出这些，检查 `core/Views.php` 是否覆盖成功。
- **手机后台里表格滑不动 / 弹层被键盘挡住？** 升级到 v1.6.1 即修复（表格改为横向滚动且首列粘住，弹层改为整屏）。旧版没有这两个处理。

- **注册功能能整个关掉吗？** 能。后台 →「站点设置 → 资源请求 → 是否开放注册」选「关闭注册」。前台注册页会显示「暂未开放注册」，已注册用户照常登录。
- **能让不注册的游客也提交吗？** 能。把「提交资源请求是否要求登录」改成「游客也能提交」即可；此时列表里会显示「（未登录提交）」，建议在「页面公告」里说明一下。
- **升级到 v1.6.2 要不要动数据库？** **不用手工动**。两张新表（`app_users` / `app_requests`）在首次打开注册页或保存一次设置时自动创建；老站点即使表已存在、只是少列，程序也会自动 `ALTER` 补齐。只有数据库用户没有建表权限时，才导入一次 `sql/migrate_users.sql`。
- **用户登录成功后又变回未登录 / 反复掉登录？** 九成是**会话目录不可写**。看 `check.php` 第 11 组的「会话（登录状态）」那一项：宝塔 → 软件商店 → PHP-x.x → 设置 → 配置修改，`session.save_path` 指向的目录必须存在且可写（一般是 `/tmp`；也可以改成站点下的 `tmp` 目录并 `chown` 给 `www`）。
- **用户忘了密码怎么办？** 目前没有邮件找回。站长在后台 →「资源请求 → 注册用户」→ 点该用户的「重置密码」，把新密码告诉他即可。
- **能不能只让部分用户提交？** 能。在「注册用户」里对不欢迎的账号点「禁用」，他就无法登录、也无法提交；他已有的请求记录仍保留在列表里供你处理。
- **想让请求页被搜索引擎收录（带点长尾流量）？** 这几个页面目前是 `noindex,follow`（功能页，不抢内页收录）。想放开就在「页面说明」里写清关键词，并把 `core/UserViews.php` 的 `head()` 第三个参数改成 `false`（即去掉 `noindex`）—— 改一处即可，三个页面共用它。
- **有人恶意刷请求怎么办？** 三层已经挡住大部分：每人每天上限、同 IP 十分钟最多 15 条、同一人重复同名挡下。仍被刷就把「每人每天最多提交几条」调到 1~2，必要时在「注册用户」里禁用对应账号，TAB 里按「待处理」逐条处理。
- **`/request` 打开是 404？** 这是新增的动态路由，需要 Nginx 伪静态生效（见第四节）。另外确认站点根目录**没有**同名的 `request` 目录或 `request.php` 文件 —— 有物理文件时 Nginx 会优先返回它，动态路由不生效。
- **顶栏出现两个「资源请求」入口？** 不会重复。导航里已经勾了它时会自动跳过，只有没勾时才补一个；实在不想要，把「顶部导航显示「资源请求」入口」设成「隐藏」即可。
- **`check.php` 第 11 组里「会话目录不可写」是 fail，会影响什么？** 只影响「登录状态能否保持」。注册、提交、后台管理都不受影响；但用户会每次都像没登录一样。修好目录权限即恢复。

- **网页安装器安全吗？会不会被人重装我的站点？** 不会。它只在"尚未安装"时可用：一旦 `config/install.lock` 存在（或 `config/config.php` 的 `db` 段已填真值），它对所有人返回 404，与文件不存在无异；提交安装表单还必须带会话 CSRF 令牌，并带蜜罐字段与失败节流。这与 WordPress 的"装完即锁"是同一套思路 —— 既让站长能在浏览器里两分钟装好，又不给任何人留后门。
- **`https://` 打开是 502 Bad Gateway，但 `http://` 正常？** 说明 Nginx 的 **443 那个 server 块里 PHP 转发坏了**（80 段是好的，所以页面出得来）。排查顺序：① 宝塔 → 网站 → 该站点 → **配置文件**，看 `listen 443` 段里的 `include enable-php-XX.conf;` 在不在、版本号是否与「网站 → 设置 → PHP 版本」一致；② 宝塔 → 软件商店 → 对应 PHP 版本 → **重启**；③ 还不行就看 `/www/wwwlogs/你的域名.error.log` 最后几行 —— Nginx 会直接写明原因（如 `connect() to unix:/tmp/php-cgi-XX.sock failed`，即 socket 不存在或版本对不上）；④ 最后可以把「网站 → 设置 → PHP 版本」重新选一次并保存，让面板重写站点配置。
