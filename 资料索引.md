# 万能资料库 · 资料索引

本资料库按 **原始源码 / 发布版本 / 发布包 / 升级包 / 软件素材** 五类归档，方便追溯与查看。

## 一、目录约定

> **扁平化（2026-09-24 起）**：五个目录**一律不套 `media-library-api/`（或任何项目名）中间层**，
> 文件直接放在目录下。对：`发布包/media-library-api-v1.9.4.zip`；
> 错：`发布包/media-library-api/media-library-api-v1.9.4.zip`。

| 目录 | 放什么 | 说明 |
|---|---|---|
| `原始源码/` | 各项目开发源码（可修改、可编译，**明文**） | **唯一改动来源**，改动从这里出发 |
| `发布版本/` | **最新版「不打包」源码**（全量文件，可直接部署、直接翻看） | **打开即见代码，不用解压**；另附 `CHANGELOG.md` / `README.md`。**PHP 已混淆** |
| `发布包/` | **整包 zip**（首次安装 / 跨版本升级用） | 一个版本一个 zip，**直接放本目录下** |
| `升级包/` | **升级包**（给在跑的站点覆盖升级） | v1.9.4 起 = 全量（明文→混淆无法增量）；历史上是增量 |
| `软件素材/` | 预览页、界面效果图、文档、**排障脚本**、图标等 | 给人看 / 给站用，不参与运行 |

### 发布版本的三条硬规则（长期有效）

| # | 规则 | 说明 |
|---|---|---|
| 1 | **不含任何测试 / 诊断脚本** | `__diag.php`、`__emergency_reset.php`、`__migrate_189.php`、`check.php` 一律不得出现在 `发布版本/` `发布包/` `升级包/`。它们放在 `软件素材/`，需要时单独上传、**用完即删** |
| 2 | **PHP 必须做免费防破解混淆** | 去注释 + 压缩空白 + 局部变量改名（仅函数/方法/闭包作用域内）；类名 / 函数名 / 方法名 / 常量 / 字符串 / 类属性名**一律不动**，接口行为完全一致。工具 `.workbuddy/obfuscate_194.mjs`（Node + php-parser 自研，免费） |
| 3 | **明文 → 混淆只能全量覆盖升级** | 逐文件增量会把明文与混淆文件混在一起 |

## 二、项目清单

### media-library-api —— 影视 / 游戏 / 动漫 资料库 API

| 项 | 位置 |
|---|---|
| 源码（明文） | `原始源码/`（**唯一改动来源**） |
| 不打包源码 | `发布版本/`（== v1.9.4 全量源码，**PHP 已混淆**，打开即见，可直接部署上线） |
| 发布包 | `发布包/media-library-api-v1.9.4.zip`（整包，首次安装用） |
| 升级包 | `升级包/media-library-api-update-v1.9.3-to-v1.9.4.zip`（v1.9.3 → v1.9.4，**最新，全量 61 文件**）；`media-library-api-update-v1.8.9-to-v1.9.0.zip`；`media-library-api-update-v1.8.8-to-v1.8.9.zip`；`media-library-api-update-v1.8.7-to-v1.8.8.zip`；`media-library-api-update-v1.8.6-to-v1.8.7.zip`；`media-library-api-update-v1.8.4-to-v1.8.5.zip`；`media-library-api-update-v1.8.2-to-v1.8.3.zip`；`media-library-api-update-v1.7.0-to-v1.8.0.zip`；`media-library-api-update-v1.6.2-to-v1.6.3.zip`；`media-library-api-update-v1.6.1-to-v1.6.2.zip`；`media-library-api-update-v1.6.0-to-v1.6.1.zip`；`media-library-api-update-v1.5.0-to-v1.6.0.zip` |
| 素材 | `软件素材/`（主页 / 内页 / 系统设置 / 安全防护 / 同步采集 / **站点设置 5 页** / **前台效果** / **注册页** / **资源请求页** / **后台资源请求** / **未安装引导页** / **安装向导** 预览、部署文档、**安装说明.txt**、**功能说明.txt**、Nginx 加固片段） |
| 排障脚本 | `软件素材/`：`check.php`（11 组自检）、`__diag.php`（登录体检）、`__emergency_reset.php`（改密码+解锁+自删）、`__migrate_189.php`（v1.8.9 迁移）—— **不随发布版本分发**，需要时单独上传、用完删除 |
| 部署文档 | `原始源码/DEPLOY.md`（含 **Nginx 伪静态 + 安全加固**、**系统设置** 与 PHP 版本支持） |
| 全新安装 | 两种装法，任选其一：① 浏览器打开 `/install.php` 按向导装（**推荐**，不用终端）；② 命令行 `php tools/install-cli.php`。两者共用同一套内核 `core/Installer.php`，**装完写入 install.lock 后 install.php 自动 404** |
| 重装 / 迁移 | `php tools/install-cli.php --force`（原 config.php 自动备份） |
| 后台入口 | `/admin` —— 用**账号 + 密码**登录（安装时创建的管理员账号） |
| 后台设置 | API Key / 数据源 / 网盘类型 → 后台 →「系统设置」（存 `app_settings` 表，优先级高于 config.php） |
| 站点设置 | 基础设置 / SEO / 扫描+跳转 / 网盘链接 / 接口配置 / **资源请求** → 后台 →「**站点设置**」（v1.6.0 起，同样存 `app_settings`） |
| 自动补下载地址 | PanSou 网盘搜索对接 → 后台 →「站点设置 → 接口配置」；采集入库后自动搜标题并写入网盘链接（v1.6.0） |
| SEO 辅助地址 | `/robots.txt`、`/sitemap.xml`（**动态路由**输出，内容在后台「站点设置 → SEO」里改） |
| 采集参数 | 每源条数 / 时间窗 / 口径 / 最低票数 / 重复处理 / 最小间隔 / 请求上限 → 后台 →「同步采集」 |
| 每小时采集 | 宝塔计划任务：Shell 脚本，`php 站点目录/cron/sync_hourly.php`，周期 1 小时（后台有可复制命令） |
| 安全防护 | 后台 →「安全防护」：安装器**三态**（未上传 / 已自锁 / 尚未安装因而可用）/ 目录拒访 / 内部守卫状态，一键打开自检报告 |
| 环境自检 | `/check.php`（或 `/check`、`/health`；从服务器本机 / 内网访问，或管理员登录态；**未授权一律 404**）—— **v1.9.4 起不再随发布版本分发**，从 `软件素材/check.php` 单独上传、用完删除 |
| 版本记录 | `发布版本/CHANGELOG.md`（含各版本增量升级包登记） |
| 当前版本 | **v1.9.4**（2026-09-24） |

**定位**：可部署在宝塔面板的 PHP + MySQL + Nginx 媒资资料库服务，对外提供
**TMDB 兼容代理**（ZBLOK 等主题零改动对接）+ **统一 API** + **可视化管理后台**。

**要点**
- **PHP 5.6 ~ 8.2 全兼容**（已全量降级 7.0+ 语法）
- 9 大分类：短剧 / 电影 / 电视剧 / 动漫 / 综艺 / 游戏 / 书籍 / 音乐 / 其他
- 主页：卡片墙 + 排序（热度 / 点击 / 最新 / 评分）+ 分类筛选 + 分页（前台不暴露 API 字样）
- 静态内页：`/uisc/{id}.html`，按类型差异化渲染；**下载区仅在填写链接时才显示**
- 多条网盘下载：夸克 / 迅雷 / 光鸭 / 百度 / UC（**后台「系统设置」里可自由增删**，无需改文件）
- 后台：9 类栏目 + 新增/编辑/删除 + **下载链接失效检查**（单条/批量）+ **系统设置** + **安全防护** + **同步采集** + 重建静态页
- 数据源适配器：TMDB（影视）、RAWG（游戏）、红果短剧（国产短剧，免 Key），可扩展
- **后台「系统设置」**：管理员令牌 / TMDB Key / RAWG Key / 启用数据源 / 网盘类型全部可视化增删改；Key 支持一键「测试连通」；改动存 `app_settings` 表，**优先级高于 config.php、升级覆盖不丢**
- **一键环境自检 `check.php`**：环境 / 权限 / 安装状态 / 数据库 / 表结构 / 路由伪静态 / 上游连通 / `php -l` 语法 / **安全防护** / **采集与计划任务** / **站点设置·SEO·网盘接口** / **注册与资源请求**，共 **11 组**，支持 `?format=json` 与 `?write=1` 写库测试；**未带正确令牌一律 404**；第 8 组会实测 `/install.php`、`/config/config.php`、`/home.php`、`/sql/install.sql` 等是否可从公网打开，并检查 `cron/*.php` 是否都带防触发守卫（v1.4.1）；**第 10 组（v1.6.0）** 检查站点设置 / SEO / `/robots.txt` 与 `/sitemap.xml` 实访问 / `uisc-assets` 可写性 / 跳转扫码配置 / PanSou 连通性与待确认队列
- **安装器与安全加固（v1.6.3 更新）**：**网页安装器 `install.php` 带自锁** —— 仅在「尚未安装」时可用；装完写入 `config/install.lock`（或 `config/config.php` 的库信息已生效）后，它立刻对所有人返回 404（与文件不存在完全一致，没人能借此重装站点）。命令行 `php tools/install-cli.php` 仍保留，两者共用同一套内核 `core/Installer.php`。`core/Guard.php` 三层防护（安装路径黑洞 + 敏感目录/后缀拦截 + 内部文件反直访，**精确放行 `/install.php` 由它自行判定**）；`config/ sql/ core/ adapters/ cron/ tools/` 各带拒访 `index.php`；`/setup`、`/upgrade`、`/update` 等路径恒 404
- **采集（同步）引擎 `core/Sync.php`（v1.5.0）**：**只取「最热」与「最多人看」**（TMDB trending + popular 榜 / RAWG -added + -rating / 红果短剧 推荐 + 排行）；**按各源官方要求限流**（TMDB 300ms、RAWG 1500ms、红果短剧 10000ms 每请求，单次请求总数与每源条数都有硬闸）；**重复就跳过**（同源同 ID + 跨源同名同年）；只给新增条目生成静态页
- **每小时采集计划任务（v1.5.0）**：`cron/sync_hourly.php`（CLI 专用，网址访问 404）；三重防重复触发（最小间隔 55 分钟自动跳过 + `config/sync.lock` 进程锁 + 请求数硬闸）；`--dry` 排错、`--force` 强跑、`--quiet` 适合日志；退出码 0/1
- **修掉「点同步没反应」（v1.5.0）**：后台接口封装改为**永不抛异常**，非 JSON 响应（PHP 致命错误页 / 502 / 504）也给出带状态码的人话提示；同步按钮加忙碌态，结果按「源 × 接口」列表展示
- **发布包不含 `config/config.php`**（只带示例 `config/config.sample.php`），配置由安装器（网页或命令行）生成，升级覆盖不会重置数据库配置
- **站点设置（v1.6.0）**：后台「站点设置」下 5 个页面 —— **基础设置**（站名 / 隐藏名称 / 宣传语 / LOGO / icon / 网站网址 / 页脚三段）、**SEO**（标题·关键词·描述·统计代码·列表页与内页标题模板·内页描述截取字数·robots.txt·sitemap）、**扫描+跳转**（PC 端三模式 + 手机端策略 + 群二维码 + 前端模板 + 顶部导航 + 榜单有图无图 + 最新列表）、**网盘链接**（按网盘分组维护 Cookie / 默认转存目录 / 临时资源目录）、**接口配置**（PanSou 对接 + 采集后自动补地址）
- **前台完整生效（v1.6.0）**：顶栏 LOGO + 可隐藏站点名 + 宣传语、**顶部导航**（后台勾选 + 自定义外链）、**经典 / 简约**双模板（简约含明暗两套配色）、**榜单有图 / 无图**、首页「**最新更新**」区块、页脚三段
- **SEO 落地（v1.6.0）**：`/robots.txt` 与 `/sitemap.xml` 改为**动态路由**输出（不再需要物理文件）；首页 / 列表页 / 内页三种上下文自动套用标题模板；统计代码原样插到每个页面 `</head>` 前
- **跳转 + 扫码（v1.6.0）**：PC 端可设「跳转+扫码 / 仅跳转 / 仅扫码」，含**降级规则**（填不全也不会把站点锁死），`手机端是否同样处理` 默认「否」；**只拦首页入口**，内页静态文件不拦，不影响收录
- **采集后自动补网盘下载地址（v1.6.0）**：对接 **PanSou** 网盘搜索；`标题相似度打分`（similar_text + 包含关系加分 + 年份加分）→ 三种写入策略（仅高度匹配自动写 / 搜到就写 / 一律进待确认）→ **按网盘去重，不覆盖站长手工填的链接** → 补到地址的条目**再生成一次静态页**
- **注册与资源请求（v1.6.2）**：前台 `/user/register`、`/user/login`、`/user/logout` 三页 + 独立页 **`/request`「资源请求」**；注册用户可提交想找的资源（片名 / 类型 / 年份 / 备注 / 联系方式），顶部导航与顶栏自动出现入口；后台新增一级页签「**资源请求**」（请求列表 + 注册用户两个二级页，可改状态、回复、删除、重置密码、禁用/解禁账号）；站点设置新增「**资源请求**」二级页（13 项开关：开放注册 / 必须登录才能提交 / 注册审核 / 图形验证 / 游客可看 / 公开列表 / 显示联系方式 / 导航入口 / 每日上限 / 最少标题字数 / 页面标题 / 页面简介 / 站长公告）；两张表 `app_users` / `app_requests` **自动创建 + 缺列自愈**，覆盖升级不用手工导 SQL；自带风控（同一 IP 每小时最多注册 3 个、登录错 5 次锁 15 分钟、每人每日提交上限、同 IP 10 分钟最多 15 条、同名去重、蜜罐字段、算术验证码）；**密码用 `password_hash()` 加盐存储，注册提示不泄露账号是否存在**；删除用户只停用并保留其历史请求
- **首页致命修复（v1.6.4）**：`home.php` 用了短类名 `Config::sub(...)` 却没有 `use Core\Config;`（全局命名空间），导致站点一装好、访问首页即 `Fatal error: Class 'Config' not found`（v1.6.0 引入，因站点从未装成而未暴露）。已补 `use Core\Config;` + 显式 require `core/Site.php`，**零行为变化、数据库无变更**；新增发布前必跑项 `phpclassscan.py`（类名解析扫描，`phpcheck`/`php56scan` 查不出这类问题）
- **后台账号登录（v1.8.0）**：后台不再用「管理令牌」卡自己 —— 改为**账号 + 密码**登录，安装时创建的第一个账号就是管理员（会话保持，不用每次填）。「系统设置」里的「后台管理令牌」项已移除；令牌从此**只属于外部调用者**（「API 调用」页发放）。`check.php` 的访问门槛同步改为「管理员登录态」。
- **后台数据源测试修复（v1.8.1）**：系统设置里三个数据源（TMDB / RAWG / Bangumi）的连通测试按钮修好 —— Bangumi 动漫源免 Key 也能直接测，并新增「一键测试全部数据源」；接口配置页 PanSou「测试连接」点击无响应也已修复（前端 `api()` 永不抛异常、后端探测接口补 `try/catch`）。
- **邮件服务与密码找回（v1.8.2）**：后台「站点设置 → 邮件服务」新增 SMTP 发信配置（服务器 / 端口 / 加密 / 账号 / 授权码），带「发送测试邮件」（支持用未保存草稿值先测再存，失败时给出 SMTP 会话日志）；后台登录页与前台登录页新增「忘记密码」—— 重置链接发进账号绑定邮箱（60 分钟有效、一次性，令牌只存 sha256 哈希）；后台右上角与前台资源请求页新增「修改密码 / 绑邮箱」。重置记录存新表 `app_password_resets`，首次使用自动建表，无需手工导 SQL。
- **MySQL 5.6 安装修复 + 邮箱即账号（v1.8.3）**：修复全新安装在 MySQL 5.6 建表报 1089 错误（`app_api_logs.endpoint` 前缀索引超出 VARCHAR(128) 列宽，改普通索引）；注册改为「邮箱即账号」——只填邮箱 + 密码，不再填用户名 / 昵称，登录 / 找回密码都用邮箱，老用户名账号兼容不变。
- **后台登录修复 + 自救工具（v1.8.6）**：修掉「输入账号密码没反应 / 提交后又跳回登录页」——① `admin/index.php` 自己就是后台登录页（合法入口），此前若被 Nginx 直接定位到该文件，会被自带守卫误判成「内部文件」而 404，现已补 `ML_APP` 定义修正；② 新增 `core/Session.php` 统一会话引导：`session.save_path` 不可写时自动改用可写目录，反代（宝塔 / CDN）下按 `X-Forwarded-Proto` 识别 HTTPS，避免 cookie 被标 Secure 后浏览器丢弃；③ 登录蜜罐字段 `website` 改名 `ml_hp`（原字段会被浏览器 / 密码管理器自动填充，触发「异常提交已忽略」，表现为登录无反应）；④ 登录提交容错（隐藏字段被代理吃掉也能登录）。另新增随包工具：`__diag.php` 登录体检页（会话 / 入口 / 账号 / 密码一站式结论）、`__emergency_reset.php` 升级为「改密码 + 解锁 + 状态复位 + 成功后自删」。
- **多选设置保存修复（v1.8.7）**：修掉「后台勾选数据源后保存，仍提示『请至少启用一个数据源』」——前端提交的是勾选数组，后端只按字符串处理，数组被 PHP 转成字面量 `Array`，校验永远通过不了（与账号密码无关）；现已正确接收数组。**同类问题一并修复**：站点设置里的多选字段（`nav_show` 顶部导航 / `pan_group` 要管理哪些网盘 / `pansou_types` 启用哪些网盘类型）此前保存后会被当成空值而清掉、退回 config.php 默认，现可正常保存；同时补齐站点点设置各组的「当前值」构建，此前打开这些页面输入框空白、勾选框不勾，现能正确回显。
- **首页热门标签云（v1.8.8）**：首页首屏由「标题 + 统计块」改为**热门标签云** —— 聚合全部条目的类型标签，按**累计点击量**降序取前 50 个；默认在区域内缓慢漂浮（每 2.8s 微漂移一次），**鼠标移入自动归位、按热度排成整齐标签墙**，移出恢复漂浮；点击标签进入 `/?tag=xxx` 列表（`CacheStore::listBySort()` / `countItems()` 新增标签筛选参数，列表页顶部显示「当前标签」+ 清除入口，排序/分类/翻页链接都会带上标签）。字号按热度分三档（前 8 名最大）。窄屏（<680px）只显示前 24 个；列表页 / 搜索页 / 翻页仍沿用原 banner。顺带修掉「最新更新」列表里误粘的一段 CSS（原先会把 4 行 CSS 当纯文本显示在页面上）。
- **API 调用令牌（v1.7.0）**：后台新增一级菜单「**API 调用**」—— 把资料库 API 开放给外部系统/个人时，为**每个调用者**发独立令牌，可分别设置**每日调用上限**与**IP 白名单**，并记录每次调用的接口 / 参数 / IP / 耗时（调用方式：请求头 `X-Api-Token` 或参数 `?token=`）。**公开接口仍可不带令牌调用**，老调用方无需改代码。与后台登录令牌（`config.php` 的 `admin.token`）彻底分开 —— 后者现在只服务于站长自己。
- **未安装引导页（v1.6.3）**：站点还没装好时访问首页**不再 404**，而是给一页引导 —— 说明缺什么 + 🚀「**开始网页安装**」按钮（指向 `/install.php`）+「命令行 / 手动」两条备选路径 + **伪静态排查提示**（若按钮仍 404，说明 Nginx 里还留着 `location ~* ^/(install|setup|upgrade|update)`，去掉 `install` 即可）。页面带 `noindex,nofollow`，并跟随系统深色。
- **伪静态**（必配）：`location / { try_files $uri $uri/ /index.php?$query_string; }`


## 三、归档动作约定

1. **改代码** → 在 `原始源码/` 内修改（**唯一明文来源**）。
2. **混淆** → 跑 `.workbuddy/obfuscate_194.mjs`，产物铺到 `发布版本/`（PHP 全混淆）。
3. **发版本** → 打包新**整包** zip 放 `发布包/`；同时把混淆后的最新源码**不打包**铺一份到 `发布版本/`
   （打开即见、可直接部署），并在 `发布版本/CHANGELOG.md` 加一段记录。
   打包前跑 `.workbuddy/build_release_194.py`，它会硬性断言：
   ① 无测试 / 诊断脚本；② 混淆确实生效；③ 整包 / 升级包 / 发布版本三方逐字节一致。
4. **做升级包** → 明文版之间可比对前后两个整包（逐文件 MD5）只发变化文件；
   **明文 → 混淆必须全量覆盖**（升级包内容 = 整包内容 + `UPGRADE.md` / `UPGRADE.txt`）。
   放 `升级包/`，并在同目录 README 登记。
5. **补素材** → 预览页 / 效果图 / 截图 / **排障脚本** 放 `软件素材/`。
6. **发布前必查** → 发布版本不得含 `__diag.php` / `__emergency_reset.php` / `__migrate_189.php` / `check.php` /
   `config/config.php` / `config/install.lock` / `preview/`；PHP 必须已混淆。
7. **新项目** → 文件直接放五个目录下（**不建项目名子目录**），并在本文件「项目清单」中登记。

---
_最后更新：2026-09-24（v1.9.4 · 目录扁平化 + 发布版本去测试脚本 + 免费防破解混淆）_
